something like:

box files:download ID 

do we want to do: ?
box files:download ID DESTINATION

this requires: `box login` (browser oauth2 login)

download files, then Py2PDF to read to text,
extract text to files, then run nlp on each file.

output structured "long" format for nlp results


